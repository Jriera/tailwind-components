import { TwButtonRoundedDirective } from './tw-button-rounded.directive';

describe('TwButtonRoundedDirective', () => {
  it('should create an instance', () => {
    const directive = new TwButtonRoundedDirective();
    expect(directive).toBeTruthy();
  });
});
